{
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "docker compose exec -T app ruff format . || ruff format . || true",
            "description": "Auto-format Python files with ruff at the end of any session. Tries Docker first (service 'app'), falls back to the local ruff CLI, swallows errors so it never blocks the user."
          }
        ]
      }
    ]
  }
}
