{
  "_comment": "Runtime plugin dependencies for python-plugin. Currently empty — python-plugin does not require external superpowers skills directly. The core sdlc plugin's runtime-dependencies.json declares cross-cutting dependencies (e.g., superpowers).",
  "dependencies": []
}
