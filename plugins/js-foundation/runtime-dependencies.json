{
  "_comment": "Runtime plugin dependencies for js-foundation. Currently empty — js-foundation does not require external superpowers skills directly. The core sdlc plugin's runtime-dependencies.json declares cross-cutting dependencies (e.g., superpowers).",
  "dependencies": []
}
